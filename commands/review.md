변경된 코드에 대해 플로우 기반 QA 리뷰를 수행합니다.
모든 판단은 **측정·실행 기반**으로만 진행합니다.
- 근거 = 코드 직접 확인 · grep 전수 결과 · 실제 실행/측정 결과. 이외는 근거 아님.
  (단, **부정 단언**의 근거 요건은 아래 채점 규칙을 따른다 — grep 1회는 "없다"의 근거가 못 된다.)
- **채점 규칙**: 추측/추론 금지. 단, **모르는 것을 모른다고 쓰는 것은 금지가 아니라 의무다.**
  - **자동 불합격 = 근거 없는 단정**. "전수 확인했다", "영향 없다", "0건", "동일하다"를
    실행한 명령·읽은 파일 없이 쓰는 것.
  - **부정 단언**("없다" / "0건" / "미사용" / "영향 없음")은 다음 2가지를 모두 채운 경우에만 허용:
    1. **탐색 범위를 문장에 명시** — 레포 · 브랜치 · 경로 · 기준 시각
    2. **성격이 다른 2경로**로 확인. 최소 1개는 **비어휘적**이어야 한다 —
       파일/브랜치 목록 열거 · 라우팅·설정 정의 확인 · `git log --all -S` · 호출그래프.
       (어휘 grep 2회는 같은 맹점을 공유하므로 2경로로 치지 않는다.)
    미충족 시 "없다"가 아니라 **"X 범위에서는 찾지 못함(미확인)"**으로 쓴다.
  - **자동 불합격 아님 = 범위 한정 서술**: "X까지만 확인, 그 밖은 미확인" — 오히려 필수 표기.
  - **여전히 금지 = 측정 가능한 값의 어림**: "대략 24px", "응답 ~200ms쯤".
- 측정 가능한 값은 실측만 인정한다. 영향 범위는 grep 결과 파일 목록을 확인한 뒤 판정한다.

## 0단계: 리뷰 범위 선언 (먼저, 한 번)

- **비교 기준**: `git fetch --all` 실행 후 base를 명시한다 (예: `origin/develop`).
  fetch 없이 원격 상태를 근거로 쓰지 않는다. 원격 관련 서술에는 **fetch 시각**을 함께 적는다.
  ⚠️ fetch로도 **아직 push되지 않은 브랜치는 보이지 않는다** — 원격 대상 부정 단언은 이 한계까지 명시한다.
- **소비자가 다른 레포에 있는가?** API · 응답 스키마 · 인증/가드 · 이벤트 변경이면 **기본값 = 예**.
  - 예 → 대상 레포 경로를 나열하고, 그 레포에서도 fetch 후 조사한다.
    접근 불가하면 결론에 **"레포 밖 미검증"**을 명시하고 그 범위를 결론에서 제외한다.
  - 아니오 → 그렇게 판단한 근거 1줄.
- **이 리뷰의 차수**: 1차인가, N차(재리뷰)인가.
  - N차면 3단계 전에 **선행 리뷰의 지적사항 ↔ 그 이후 커밋**을 먼저 대조한다.
  - 선행 리뷰에서 가져오는 값은 전부 `인용`으로 표기한다 (→ 출력 형식).

## 1단계: 변경 파일 파악

0단계에서 선언한 base 기준으로 `git diff --name-only <base>...HEAD`를 실행해 변경 파일 목록을 확인합니다.
변경 파일에서 추가/수정된 함수, API, 타입을 식별합니다.

## 2단계: grep 전수 확인

변경된 함수/API/타입 이름으로 프로젝트 전체 grep을 실행하여 **모든 호출 위치**를 파악합니다.
- 동일 함수가 여러 곳에서 호출되면 중복/불필요 여부 교차 비교
- 판정 근거로 삼는 파일은 전체 Read. 목록만 훑은 파일은 "목록만 확인"이라고 명시한다
  (부분 읽기만으로 "전체 정상" 판단 금지).
- 변경된 이벤트/상태명으로 **수신자(리스너/핸들러/구독자/캐시 무효화)**도 전수 파악.
  변경 파일 밖의 기존 핸들러가 이번 변경 맥락에서 충분한지 교차 검증.
- 0단계에서 "타 레포 소비자 있음"으로 판단했으면 **그 레포에도 동일 grep을 반복**한다.

## 3단계: 플로우 리스트업

변경 내용을 유저 액션 기준 플로우로 분류합니다.
각 플로우: `유저 액션 → 핸들러 → API/서비스 → DB → 응답/이벤트`

## 4단계: 플로우별 코드 추적 검증

각 플로우마다:
1. **중복 호출**: 동일 API/함수가 불필요하게 여러 번 호출되지 않는지
2. **타입 일치**: 데이터가 전달되는 모든 경로에서 타입/필드가 일치하는지
3. **필드 누락**: 새로 추가한 필드가 전체 체인에 포함되는지
4. **기능 정합성**: 의도한 데이터만 변경되고, 불필요한 부수효과 없는지
5. **부수 효과 전파**: 변경된 흐름이 알림/이벤트/로그/메트릭/소켓 브로드캐스트 등 외부 시스템으로 나가는 경우, 변경된 조건(예: 필터 제거·권한 완화) 하에서 그 부수 효과가 **의도한 범위**로 확장됐는지 정책 판단. 확장이 부적절하면 플래그.
6. **대칭 분기 검증**: 상태 전이가 쌍으로 구성되는 경우(생성↔삭제, 활성↔비활성, 로그인↔로그아웃, 추가↔제거), **반대 분기가 동일 품질로 처리됐는지** 확인. 한쪽만 구현된 핸들러·캐시 무효화·토스트는 플래그.
7. **관련 핸들러 완결성**: 변경된 도메인을 다루는 **기존 리스너/핸들러/캐시 경로**(변경 파일 밖이어도)가 이번 변경의 맥락에서 여전히 충분한지. 2단계에서 파악한 수신자 목록을 기반으로, 각 수신자가 변경 후에도 정상 반응하는지 코드 직접 확인.
8. **Ripple Check (변경 속성 ↔ 미변경 속성 간 상호작용 재검증)**: 이번 변경 E로 인해 **E의 변경되지 않은 속성**이 **E의 이웃 N**과 맺는 관계가 달라지는 경우를 포함해 재확인.
   - **이웃 N 식별 (도메인별)**:
     - FE: 같은 viewport/컨테이너에서 공간적으로 충돌 가능한 fixed/absolute/sticky 요소, 같은 z-index 층, 같은 레이아웃 컨테이너 형제·조상, 같은 state/store 구독자
     - BE: 변경된 심볼명으로 grep해서 나오는 모든 caller/callee, 같은 이벤트·DB 엔티티·캐시 키·인덱스를 공유하는 경로, 동일 미들웨어/가드가 적용되는 라우트 집합
   - ⚠️ **위 목록은 예시이지 완결 목록이 아니다.** 목록에 없는 축(다른 레포 · 미fetch 원격 ref · 배포 순서)을 탐색 대상에서 빼지 말 것.
   - **재검증 요점**: E의 변경된 속성 + 미변경 속성이 함께 작용해, N의 기존 불변식(layout invariant · API contract · 성능 가정 · 권한 경계)을 여전히 성립시키는지
   - **맹점 패턴**:
     - FE: "bottom 값은 유지됐지만 right가 바뀌며 다른 fixed 요소와 공간 겹침 발생"
     - BE: "응답 스키마는 유지됐지만 enum 값 의미·nullability가 바뀌어 consumer 해석 달라짐", "where 조건은 바뀌었는데 해당 컬럼 인덱스가 없어 다른 쿼리 성능까지 저하"
   - **판정**: "변경 안 건드림 → OK" 자동 종결 금지. 반드시 이웃과의 상호작용까지 확인 후 판정.

## 5단계: 중복 코드 / 리팩토링

- 동일 패턴이 3곳 이상 반복되면 공통 함수 추출
- 오버엔지니어링 금지 — 추출이 코드를 더 복잡하게 만들면 그대로 둠

## 6단계: 실행 검증 (추정 금지, 실제 실행만 인정)

- **공통**: 빌드/타입체크 실행 후 에러 0건 확인 (명령어와 결과를 출력에 명시)
- **UI 변경 포함 시 (프론트엔드)**:
  - 검증 viewport는 **변경 영향 범위**에 맞춰 선택:
    - 반응형 / 레이아웃 컨테이너 / 전역 레이아웃 관련 → 모바일(≤480px) + 태블릿(~768px) + 데스크탑(≥1280px) 3종 모두
    - 특정 단일 브레이크포인트에서만 렌더되는 컴포넌트 → 해당 브레이크포인트만
    - 불확실하면 → 3종 모두
  - dev server가 **이미 기동 중이면** 사용자에게 실제 렌더 확인을 **명시적으로 요청** (AI가 새로 기동하지 말 것)
  - dev server가 **꺼져 있으면** 기동 여부를 **사용자에게 먼저 확인**
  - 실물 확인 없이 "시각 이슈 없음" 단정 금지. 확인 불가 시 "시각 검증 미수행, 사용자 확인 필요"로 명시
- **API/DB/이벤트 변경 포함 시 (백엔드)**:
  - 관련 단위/통합 테스트 실행. 없으면 smoke 테스트 1개 추가 제안
  - DB 쿼리 변경 시 실행 계획(EXPLAIN) 또는 인덱스 사용 여부 확인

## 7단계: 격리 반증 (머지 차단 결론 · 부정 단언에 한해)

- 대상은 두 가지뿐이다: **머지를 막는 결론**, **결론이 의존하는 부정 단언**(뒤집히면 판정이 바뀌는 것).
  그 외에는 하지 않는다 — 상단 채점 규칙의 2경로 요건을 충족한 부정 단언까지 반복 검증하지 않는다.
- 각 대상마다 "이 결론이 틀렸다면 무엇이 관찰되겠는가"를 1줄 쓰고,
  **가장 싼 반증 관찰 1개를 실제로 실행**한다. 반증에 실패해야 결론을 확정한다.
- **가능하면 별도 컨텍스트(서브에이전트)** 에 넘겨 수행한다 — 모델은 자기 산출물에 더 높은
  확신을 부여하므로(ownership bias) 같은 대화 안의 자기검증은 구조적으로 약하다.
- 반증 결과가 원 결론과 충돌하면 **자동으로 뒤집지 말고** 양쪽 근거를 병기해 사용자 판단에 올린다.
- **결론이 유지되더라도 근거가 바뀌었으면 그 사실을 산출물에 명시**한다.

## 출력 형식

해당 없는 섹션은 생략한다. 단 **리뷰 범위**와 **확인하지 못한 것**은 항상 남긴다(없으면 "없음").

### 리뷰 범위 (0단계 결과, 3줄)
- 비교 기준: `<base>...HEAD` / fetch 시각:
- 타 레포 소비자: (경로 나열 또는 "없음 + 근거")
- 차수: 1차 / N차

### 플로우별 검증 결과
| 플로우 | 결과 | 비고 |
|--------|------|------|

### 실행 검증 결과
| 항목 | 명령/방법 | 실행 시점 | 결과 |
|------|-----------|-----------|------|
| 빌드/타입체크 | e.g. `tsc --noEmit` | | |
| UI 시각 검증 (해당 시) | viewport별 브라우저 확인 또는 사용자 요청 | | |
| 테스트 (해당 시) | e.g. `pnpm test <file>` | | |

- **실행 시점** 표기: `이번 리뷰에서 실행` / `N차 리뷰 인용(재측정 안 함)` / `미실행(사유)`
- 선행 리뷰의 수치를 다시 실을 때는 반드시 **`인용`으로 표기**한다.
  재측정 없이 자기 측정치처럼 적으면 **자동 불합격** (→ 상단 채점 규칙).
- 인용값이 이번 리뷰의 측정과 충돌하면 **측정이 우선**하고, 충돌 사실을 산출물에 남긴다.

### 격리 반증 결과 (7단계 수행 시)
| 대상 결론 | 틀렸다면 관찰될 것 | 반증 관찰(실행) | 결과 |
|-----------|--------------------|-----------------|------|

### 발견된 이슈 (있는 경우)
| 이슈 | 위치(앵커) | 근거 | 수정 방법 |
|------|------------|------|-----------|

- **위치는 앵커(심볼·함수·데코레이터·유일 문자열)를 먼저** 적고 라인은 힌트로만 붙인다.
  예: `proxy.ts › PAYMENT_ALLOWED_PREFIXES (~22행)`. 라인번호 단독 표기 금지.
- 라인번호는 **작업 트리의 실제 파일을 열어 확인한 값**만 적는다.
  `git show`/`git diff`의 `@@` 헤더에서 옮겨 적지 않는다 — 컨텍스트 오프셋으로 어긋난다.
- 원격 PR 리뷰는 **커밋 SHA 고정 링크**를 쓴다 (작성자가 커밋을 더 밀어도 안 깨진다).
- 근거가 흩어져 있으면 흩어진 대로 모두 적는다: `proxy.ts:22, 162-170, 174-175`.

### 확인하지 못한 것 (없으면 "없음")
| 미확인 항목 | 왜 확인 못 했나 | 뒤집히면 무엇이 바뀌나 |
|-------------|-----------------|------------------------|

### 리팩토링 (수행한 경우)
| 항목 | Before | After |
|------|--------|-------|
